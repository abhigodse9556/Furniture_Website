rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All shop data is read/written only via Express + Firebase Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
